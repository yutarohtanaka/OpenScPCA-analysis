# This is a workflow to build the docker image for the celltype-annotation-osteosarcoma module
#
# Docker modules are run on pull requests when code for files that affect the Docker image have changed.
# If other files are used during the Docker build, they should be added to `paths`
#
# At module initialization, this workflow is inactive, and needs to be activated manually

name: Build docker image for celltype-annotation-osteosarcoma

concurrency:
  # only one run per branch at a time
  group: "docker_celltype-annotation-osteosarcoma_${{ github.ref }}"
  cancel-in-progress: true

on:
  # pull_request:
  #   branches:
  #     - main
  #   paths:
  #     - "analyses/celltype-annotation-osteosarcoma/Dockerfile"
  #     - "analyses/celltype-annotation-osteosarcoma/.dockerignore"
  #     - "analyses/celltype-annotation-osteosarcoma/renv.lock"
  #     - "analyses/celltype-annotation-osteosarcoma/conda-lock.yml"
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - "analyses/celltype-annotation-osteosarcoma/Dockerfile"
  #     - "analyses/celltype-annotation-osteosarcoma/.dockerignore"
  #     - "analyses/celltype-annotation-osteosarcoma/renv.lock"
  #     - "analyses/celltype-annotation-osteosarcoma/conda-lock.yml"
  workflow_dispatch:
    inputs:
      push-ecr:
        description: "Push to AWS ECR"
        type: boolean
        required: true

jobs:
  test-build:
    name: Test Build Docker Image
    if: github.event_name == 'pull_request' || (contains(github.event_name, 'workflow_') && !inputs.push-ecr)
    runs-on: ubuntu-latest

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: "{{defaultContext}}:analyses/celltype-annotation-osteosarcoma"
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-push:
    name: Build and Push Docker Image
    if: github.repository_owner == 'AlexsLemonade' && (github.event_name == 'push' || inputs.push-ecr)
    uses: ./.github/workflows/build-push-docker-module.yml
    with:
      module: "celltype-annotation-osteosarcoma"
      push-ecr: true
